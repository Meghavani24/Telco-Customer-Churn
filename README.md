# Telco-Customer-Churn
This project predicts telecom customer churn using Machine Learning. Data was cleaned, hidden missing values handled, categorical features encoded, and numerical features scaled. Logistic Regression, Random Forest, and XGBoost were trained and evaluated. Logistic Regression performed best. The model was deployed via FastAPI to serve predictions.
